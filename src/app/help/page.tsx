import { DiaryPage } from "@/components/diary-page";

/** HTML entry. AppShell swaps Help in without unloading the session. */
export default function HelpPage() {
  return <DiaryPage>{null}</DiaryPage>;
}
